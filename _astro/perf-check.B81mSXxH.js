function r(){const e=typeof navigator<"u"&&/^((?!chrome|android).)*safari/i.test(navigator.userAgent),t=typeof CSS<"u"&&CSS.supports("animation-timeline","scroll()");(e||!t)&&(document.documentElement.setAttribute("data-low-perf","compat"),console.warn(e?"🦁 Compatibility Mode: Safari detected. WebKit has known issues with complex SVG filters, so the Brain Diagram switched to text mode with animations off. Everything else is business as usual.":"🦁 Compatibility Mode: Your browser doesn't support scroll-driven animations yet. Brain Diagram is in text mode with animations disabled. Everything still works, just calmer."))}r();function s(){if(document.documentElement.hasAttribute("data-low-perf"))return;let e=0,t=0;const n=a=>{t===0&&(t=a),e++;const i=a-t;if(i<1e3){requestAnimationFrame(n);return}const o=e*1e3/i;console.log(`⏱ Perf Check: ${o.toFixed(1)} fps over ${i.toFixed(0)} ms`),o<50&&(document.documentElement.setAttribute("data-low-perf","fps"),console.warn(`🐢 Low Performance Mode: Only ${o.toFixed(0)} fps detected — your device is doing its best and we respect that. Hero area switched to a simple fade-in, Brain Diagram is in text mode with animations off. Same great content, just lighter on the GPU.`))};requestAnimationFrame(n)}document.readyState==="complete"?s():window.addEventListener("load",s,{once:!0});
